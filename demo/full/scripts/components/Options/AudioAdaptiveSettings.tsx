import * as React from "react";

const { Fragment } = React;

/**
 * @param {Object} props
 * @returns {Object}
 */
function AudioAdaptiveSettings(): JSX.Element {
  return (
    <Fragment>
    </Fragment>
  );
}

export default React.memo(AudioAdaptiveSettings);
